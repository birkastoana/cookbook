

<h1>Recipes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td> <%= recipe.title %></td>
        <td><%= link_to 'Show', recipe %></td>
        <td></td>
        <td></td>
<!--        <td><%#= link_to 'Destroy', recipe_path(recipe), method: :delete, data: {confirm: 'Are you sure?'} %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<table>
  <tr>
    <td><%= link_to 'New Recipe', new_recipe_path %></td>
  </tr>
  <tr>
    <td>
  </tr>
</table>